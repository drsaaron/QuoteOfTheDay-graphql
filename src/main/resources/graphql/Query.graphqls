type Query {
  # get a quote
  getQuote(number: Int!): Quote!

  # get quote of the day 
  getQuoteOfTheDay(runDate: Date): QuoteOfTheDay!

  # get list of quotes for a source code
  getQuotesBySourceCode(sourceCode: Int!): [Quote]
}
